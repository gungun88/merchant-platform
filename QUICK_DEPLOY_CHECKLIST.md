# ⚡ 快速部署执行清单

## 🎯 第一步: Supabase 执行 SQL (必须先做!)

### 打开 Supabase Dashboard
```
https://supabase.com/dashboard
→ 选择你的项目
→ 进入 SQL Editor
```

### 执行修复脚本 1: 积分重复问题
**复制并执行:** `scripts/999_fix_duplicate_points.sql`

**等待显示:**
```
✅ 积分重复问题修复完成
触发器已更新，现有用户积分已修正
```

### 执行修复脚本 2: 新用户注册问题
**复制并执行:** `scripts/999_fix_new_user_registration.sql`

**等待显示:**
```
✅ 新用户注册修复完成
所有触发器已重建，现有用户数据已修复
```

---

## 🧪 第二步: 本地测试验证

### 测试 1: 检查用户数据
```bash
node scripts/check_user_issues.js
```
**预期:** `✅ 所有用户数据正常!`

### 测试 2: 批量转账
1. 打开 http://localhost:3002/admin/users
2. 点击"批量转账",转 10 积分给所有用户
3. **检查积分是否到账**

### 测试 3: 新用户注册
1. 注册一个新用户
2. **检查积分是否为 100**(不是 200)
3. **检查用户编号是否存在**

---

## 📦 第三步: 提交代码

```bash
git add .
git commit -m "fix: 修复批量转账和新用户注册问题"
git push origin main
```

---

## 🚀 第四步: VPS 部署

### SSH 到 VPS
```bash
ssh your-username@your-vps-ip
```

### 执行部署命令
```bash
cd /path/to/your/project
git pull origin main
npm install
rm -rf .next
npm run build
pm2 restart your-app-name
pm2 logs your-app-name --lines 20
```

---

## ✅ 第五步: 生产环境验证

### 验证 1: PM2 状态
```bash
pm2 status
```
**预期:** `online`

### 验证 2: 批量转账
访问 https://merchant.doingfb.com/admin/users
**执行小额转账测试**

### 验证 3: 新用户注册
**注册测试账号,检查积分和用户编号**

---

## 🎉 完成!

全部完成后,所有问题都已修复! ✅
